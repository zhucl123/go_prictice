
// 题目
// 给你一个整数数组 nums ，找到其中最长严格递增子序列的长度。
//
// 子序列是由数组派生而来的序列，删除（或不删除）数组中的元素而不改变其余元素的顺序。例如，[3,6,2,7] 是数组 [0,3,1,6,2,2,7] 的子序列。

package main

import "fmt"

func TEST_DO_NOT_CHANGE(nums []int) int {
    var rlt int
    // start下面可以改动
    

    // end上面可以改动
    return rlt
}

func main() {
    fmt.Println(TEST_DO_NOT_CHANGE([]int{10, 9, 2, 5, 3, 7, 101, 18}))
    fmt.Println(TEST_DO_NOT_CHANGE([]int{0, 1, 0, 3, 2, 3}))
    fmt.Println(TEST_DO_NOT_CHANGE([]int{7, 7, 7, 7, 7, 7, 7}))
}
