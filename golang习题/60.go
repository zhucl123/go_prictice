
// 题目
// 给定一个包含红色、白色和蓝色，一共n个元素的数组，原地对它们进行排序，使得相同颜色的元素相邻，并按照红色、白色、蓝色顺序排列。
// 此题中，我们使用整数 0、1 和 2 分别表示红色、白色和蓝色。

package main

import "fmt"

func TEST_DO_NOT_CHANGE(nums []int) {
    fmt.Println(nums)
    var lst_rlt []int
    // start下面可以改动



    // end上面可以改动
    return lst_rlt
}

func main() {
    fmt.Println(TEST_DO_NOT_CHANGE([]int{2, 0, 2, 1, 1, 0}))
    fmt.Println(TEST_DO_NOT_CHANGE([]int{2, 0, 1}))
    fmt.Println(TEST_DO_NOT_CHANGE([]int{0}))
}
