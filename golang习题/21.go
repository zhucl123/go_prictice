
// 题目：最大公约数（GCD）

// 编写一个程序，接受两个正整数作为输入，并计算它们的最大公约数（GCD）。
// 最大公约数是两个或多个整数的共有因子中的最大值。例如，18 和 24 的最大公约数是 6。
// 编写一个函数 TEST_DO_NOT_CHANGE(a, b)，它接受两个正整数 a 和 b 作为参数，返回它们的最大公约数。

package main

import "fmt"

func TEST_DO_NOT_CHANGE(a, b int) int {
    x := 0
    // start下面可以改动  


    // end上面可以改动 
    return x
}

func main() {
    fmt.Println(TEST_DO_NOT_CHANGE(16, 256))
    fmt.Println(TEST_DO_NOT_CHANGE(27, 36))
    fmt.Println(TEST_DO_NOT_CHANGE(55, 121))
}
