
// 题目
// 给定一个仅包含数字 2-9 的字符串，返回所有它能表示的字母组合。
// 给出数字到字母的映射如下（与电话按键相同）。注意 1 不对应任何字母

package main

import (
	"fmt"
)

func TEST_DO_NOT_CHANGE(num string) []string {
    fmt.Println(num)
    var rlt []string
    // start下面可以改动 
    
    

    
    // end上面可以改动
    return rlt
}

func main() {
    fmt.Println(TEST_DO_NOT_CHANGE("23"))
    fmt.Println(TEST_DO_NOT_CHANGE("45"))
    fmt.Println(TEST_DO_NOT_CHANGE("47"))
}
