
// 题目：计算中缀表达式
//
// 中缀表达式是我们通常用于数学表达式的形式，例如 2 + 3。编写一个 Go 程序，接受一个包含中缀表达式的字符串作为输入，计算并输出表达式的结果。
//
// 编写一个函数 TEST_DO_NOT_CHANGE(expression)，它接受一个中缀表达式字符串 expression 作为参数，返回表达式的计算结果。
//
// 例如，如果输入字符串是 "2 + 3"，程序应该返回 5，如果输入字符串是 "3 + 4 * 2"，程序应该返回 11。

package main

import (
	"fmt"
)

func TEST_DO_NOT_CHANGE(input string) (output float64) {
    //start下面可以改动
    
    
    
    //end 上面可以改动 
    return output
}

func main() {
    fmt.Println(TEST_DO_NOT_CHANGE("2 + 3"))
    fmt.Println(TEST_DO_NOT_CHANGE("3 + 4 * 2"))
}
