
// 题目
// 给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出和为目标值的那两个整数，并返回它们的数组下标。
// 你可以假设每种输入只会对应一个答案。但是，数组中同一个元素在答案里不能重复出现。
// 你可以按任意顺序返回答案。

package main

import "fmt"

func TEST_DO_NOT_CHANGE(nums []int, target int) []int {
    var lst_rlt []int
    // start下面可以改动
    

    // end上面可以改动
    return lst_rlt
}

func main() {
    fmt.Println(TEST_DO_NOT_CHANGE([]int{2, 7, 11, 15}, 9))
    fmt.Println(TEST_DO_NOT_CHANGE([]int{3, 2, 4}, 6))
    fmt.Println(TEST_DO_NOT_CHANGE([]int{3, 3}, 6))
}
