
// 函数代码请只在 "
// start 下面可以改动

// end 上面可以改动 "

package main

import "fmt"

func TEST_DO_NOT_CHANGE(sequence string, word string) int {
    fmt.Println(sequence, word)
    var rlt int
    // start 下面可以改动





    
    // end 上面可以改动
    return rlt
}

func main() {
    sequence := "ababc"
    word := "ab"
    fmt.Println(TEST_DO_NOT_CHANGE(sequence, word))
    // 下面可以添加测试语句
}
