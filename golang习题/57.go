
// 给定一个包含 [0, n]中n个数的数组 nums ，找出 [0, n] 这个范围内没有出现在数组中的那个数。
//
// 进阶：
//
// 你能否实现线性时间复杂度、仅使用额外常数空间的算法解决此问题?

package main

import "fmt"

func TEST_DO_NOT_CHANGE(nums []int) int {
    var rlt int
    // start下面可以改动



    
    // end上面可以改动
    return rlt
}

func main() {
    fmt.Println(TEST_DO_NOT_CHANGE([]int{9, 6, 4, 2, 3, 5, 7, 0, 1}))
    fmt.Println(TEST_DO_NOT_CHANGE([]int{0, 1}))
    // 下面可以添加测试语句
}
