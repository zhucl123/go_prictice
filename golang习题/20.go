
// 题目：计算幂次方

// 编写一个程序，计算并输出给定底数 x 和指数 n 的幂次方结果。

// 幂次方的定义如下：
// x 的 0 次幂是 1。
// x 的正整数 n 次幂是 x 乘以自己 n 次，即 x^n = x * x * ... * x (n 次)。
// 例如，2 的 3 次幂是 2^3 = 2 * 2 * 2 = 8。

package main

import "fmt"

func TEST_DO_NOT_CHANGE(x, n int) int {
    var result int
    // start下面可以改动

    
    // end上面可以改动
    return result
}

func main() {
    fmt.Println(TEST_DO_NOT_CHANGE(2, 3))  // 应该输出 8
    fmt.Println(TEST_DO_NOT_CHANGE(3, 3))  // 应该输出 27
    fmt.Println(TEST_DO_NOT_CHANGE(5, 2))  // 应该输出 25
}
