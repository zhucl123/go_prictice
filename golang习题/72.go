
// 题目
// 给你一个长度为 n 的整数数组 nums 和 一个目标值 target。
// 请你从 nums 中选出三个整数，使它们的和与 target 最接近。
// 返回这三个数的和。
// 例如：数组nums = [-1, 2, 1, -4],目标值target = 1，和值最接近target的是[-1,2,1]和值为2, 在输出结果result中只需要返回 2 即可。
// 求以下几个数组中三元素和值最接近target并输出和值。
// num1 = [1,2,3,4,5], target1 = 10
// num2 = [-1,0,1,2,4],target2 = 8
// num3 = [4,0,5,1,-2],target3 = 1

package main

import (
    "fmt"
    "math"
)

func TEST_DO_NOT_CHANGE(nums []int, target int) int {
    fmt.Println(nums, target)

    // start下面可以改动


    // end上面可以改动
    return result
}

func main() {
    num1 := []int{1, 2, 3, 4, 5}
    target1 := 10

    num2 := []int{-1, 0, 1, 2, 4}
    target2 := 8

    num3 := []int{4, 0, 5, 1, -2}
    target3 := 1
    fmt.Println(TEST_DO_NOT_CHANGE(num1, target1))
    fmt.Println(TEST_DO_NOT_CHANGE(num2, target2))
    fmt.Println(TEST_DO_NOT_CHANGE(num3, target3))
}
