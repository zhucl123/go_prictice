
// 给你一个 严格升序排列 的正整数数组 arr 和一个整数 k 。

// 请你找到这个数组里第 k 个缺失的正整数。

package main

import "fmt"

func TEST_DO_NOT_CHANGE(arr []int, k int) int {
    print(arr, k)
    var rlt int
    // start 下面可以改动


    // end 上面可以改动
    return rlt
}

func main() {
    arr := []int{2, 3, 4, 7, 11}
    k := 5
    fmt.Println(TEST_DO_NOT_CHANGE(arr, k))
    // 下面可以添加测试语句
}
