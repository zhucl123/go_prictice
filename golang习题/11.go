
// 题目
// 给你一个字符串 s，由若干单词组成，单词之间用空格隔开。返回字符串中最后一个单词的长度。如果不存在最后一个单词，请返回 0。
// 单词 是指仅由字母组成、不包含任何空格字符的最大子字符串。

package main

import (
	"fmt"
)

func TEST_DO_NOT_CHANGE(s string) int {
	fmt.Println(s)
	length := 0
	// start下面可以改动



	// end上面可以改动
	return length
}

func main() {
	fmt.Println(TEST_DO_NOT_CHANGE("Hello World"))
	fmt.Println(TEST_DO_NOT_CHANGE(" "))
}
